newList = [1, 2, 3, 4, 5]       # initialize new list
print(newList)                  # prints out the list

for num in range(1, len(newList) + 1): # readds every element in the list
    newList.append(num)
print(newList)                  # print out the list